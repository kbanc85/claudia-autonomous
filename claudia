#!/usr/bin/env python3
"""
Claudia CLI launcher.

This wrapper should behave like the installed `claudia` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from claudia_cli.main import main
    main()
